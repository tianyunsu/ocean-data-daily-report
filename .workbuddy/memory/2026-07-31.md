# 2026-07-31 工作日志

## 海洋AI日报自动化执行
- HTML简报已生成（16条，9方向全覆盖），位于 daily_reports/海洋AI简报_2026-07-31.html
- 已复制至 posts/2026-07-31.html，更新 index.html、archive.html、sitemap.xml
- Git push 成功（561faf9），GitHub Pages自动部署中
- 远程已有7月多份日报（由supplement分支机器发布07-01至07-28），rebase合并时解决index/archive冲突

## 质检发现与修复（苏老师反馈）
**问题**：07-31自动执行 vs 07-28手动执行对比，暴露出三大问题：
1. **顶会检索严重遗漏**：07-28有6条顶会论文（ECCV/CVPR），07-31仅1条且与上期重复
2. **去重失败**：MARIS(CVPR 2026)、WavyOcean 3.0、CMEMS Argo QC3在上期已报道
3. **记忆文件断层**：6-7月无.memory日志，去重基准停在05-29

**已执行的修复**：
1. ✅ MEMORY.md 去重基准已补全6-7月数据（06-01至07-31共14期）
2. ✅ 自动化prompt重构为6阶段结构，阶段1强制顶会检索（23个会议+7种检索方法）
3. ✅ ocean-daily-report skill 同步更新：新增阶段一顶会检索、阶段六记忆写入、共享指令源说明
4. ✅ skill 中的9方向搜索策略表更新：每个相关方向标注对应的顶会来源

## 手动/自动一致性改造（苏老师目标：手动执行记忆与自动一致，不论频率）
**检查结论**：07-24、07-28 手动执行仅完成核心产出（posts/ + git push），漏写 3 类记忆文件：
- `daily_reports/海洋AI简报_YYYY-MM-DD.html` 源文件（daily_reports/ 从 06-26 直接跳到 07-31）
- `.workbuddy/memory/YYYY-MM-DD.md` 每日日志（7月仅 07-31 一条）
- `.workbuddy/automations/ai/memory.md` 执行记录

**已完成的修复（确保未来手动=自动）**：
1. ✅ 强化 skill「一致性铁律」：手动触发 ≠ 简化执行，必须产出与自动完全相同的工件与记忆文件
2. ✅ 重写 skill 阶段六：明确列出手动/自动一致写入的全部记忆文件（每日日志、MEMORY.md去重基准、automations/ai/memory.md 可选）
3. ✅ 自动化 prompt 改为对 skill 的纯委托（移除内联重复细节），杜绝双源漂移——自动与手动现在共用同一权威流程定义
4. ✅ 补写 2026-07-24.md 与 2026-07-28.md 每日日志，消除记忆断层

**机制保证**：今后无论手动还是定时，入口都是"加载 ocean-daily-report skill → 6 阶段"，记忆写入在阶段六统一落盘，手动与自动无差异。

## 沉淀可复用 skill（苏老师确认"需要"）
- ✅ 新建用户级 skill `automation-consistency`（~/.workbuddy/skills/automation-consistency/SKILL.md）
- 内容：手动/定时自动化执行一致性方法论——诊断断层（三类文件交叉比对）→ 强化 skill 铁律+阶段六 → 自动化 prompt 纯委托 → 补写历史日志 → 验证
- 作用：今后任何带 automations 的定时任务（日报/周报/定期抓取）碰到"手动漏写记忆"都可复用此 skill
- 同步在 MEMORY.md 新增「一致性机制」小节记录本系统的固化约定

## 跨机器执行支持（苏老师提出要在另一台电脑手动执行）
- 诊断：`.workbuddy/memory|automations` 已在 git 跟踪（32 文件），记忆可随仓库同步；唯一缺口是 skill 在用户级 `~/.workbuddy/skills/`，不随仓库走
- ✅ 把 `ocean-daily-report`(8文件) 与 `automation-consistency`(1文件) 副本纳入仓库 `.workbuddy/skills/`
- ✅ 新增 `sync_skills.py`：install（仓库→用户目录）/ collect（用户目录→仓库）/ status（比对差异），MD5 比对，跨平台
- ✅ 新增 `SETUP_NEW_MACHINE.md`：三类资产位置表、首次配置 4 步、每次执行标准动作、skill 回收流程、5 条铁律、6 类常见问题
- ✅ skill 一致性铁律段新增「跨机器铁律」：执行前 pull+install、执行后 push、禁止并行、非主力机改 skill 须 collect
- ✅ 提交推送 commit `3514a0b`（同时补交 05-28/05-29/06-01/07-24/07-28/07-31 六份记忆日志）
- 关键坑：不 pull 就检索 → 去重基准过期 → 必然重复收录；两机同日并行 → git 冲突 + 内容重复
