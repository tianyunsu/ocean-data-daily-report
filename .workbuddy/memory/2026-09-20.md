# 2026-09-20 海洋AI日报执行日志（自动化）

## 结果概览
- 期数：2026-09-20（周日），27 条有效 + 1 条空方向备注，8/9 方向有内容（方向二数字孪生仅备注、无独立新条目？实际备注在方向二）。
- 发布：`posts/2026-09-20.html` + index/archive 更新，commit `defc0ef` 已推送 GitHub Pages。
- 飞书：`feishu_write_doc.py` 404（文档 token KYOkd4j0XoXPbmxdIE5ciLSsn3g 失效）——**待用户更新 token**；机器人通知脚本跑完（邮件未配置跳过）。
- 执行环境注意：默认 `python` 无 requests，须用 `py -3` 运行 feishu_write_doc.py / run_daily_report.py。

## 五项质量审查
1. **链接有效性/摘要一致性**：27 条外链全部经 WebFetch 或 verify_paper/Crossref 核实；2 条 doi.org 返回 Client Challenge 页由 Crossref/OpenAlex 确认元数据（B 组规则放行）。
2. **时效性**：27 条中 25 条 ≤14 天；2 条按重要性豁免（≤60 天）保留并如实标注日期：
   - UN Ocean Decade《海洋数据共享与国家安全》讨论稿（08-27，24 天；调查截止 9 月底，重要行动项）
   - CMEMS OSR10 发布预告（09-04，16 天；9-30 重大发布）
3. **来源覆盖矩阵打卡**：A 组 arXiv(17链)、出版商 Frontiers/MDPI/Science 合作刊、CMEMS、EU 新闻室/UN Ocean Decade、中文媒体（中新社/腾讯/网易转载）✅；**零覆盖 5 组 ≤6 达标**：B 组商业出版商（Elsevier/Springer/Wiley/IEEE/T&F）、DOAJ、GitHub/PyPI/conda、标准组织（W3C/ISO/OGC）、CNKI/万方/维普。
4. **归类与条数**：方向一 5 / 二 备注条 / 三 3 / 四 4 / 五 5 / 六 2 / 七 2 / 八 3 / 九 3，总计 27 有效。方向二仅备注（D2 空），符合"无新内容不凑数"。
5. **去重**：31 个候选标识符逐一 `grep -rl posts/`，3 个命中（ditto_summit2026、EX2606、Ocean State Report 旧期）经上下文核查剔除/替换；MEMORORY.md 基准 12 期窗口比对无漏。

## 覆盖矩阵打卡明细
- ✅ arXiv 全域（ao-ph/cs/cs.RO 多 cat 联合查询）
- ✅ 商业出版商直查（B 组经 WebSearch+Crossref 代理）：Frontiers in Marine Science、JMSE/RS（MDPI）、Journal of Remote Sensing（Science 合作刊）
- ✅ 数据门户：CMEMS（Sentinel-3C、OSR10、WAV 公告）
- ✅ 政策/共享：EU newsroom（EMODnet/UN Ocean Decade 讨论稿）
- ✅ 中文媒体：中新社（中国海洋发展指数 132.3）、腾讯新闻、新华社（网易转载）
- ❌ 零覆盖 5 组（补检后仍无命中，如实记录）：Elsevier/Springer/Wiley/IEEE/T&F/SAGE/ACS/RSC/ACM/IOP、DOAJ、GitHub/PyPI/conda/DockerHub、W3C/ISO/OGC/CF/RDA/OBP、CNKI/万方/维普

## 待办/移交
- 飞书文档 404 → 需要新文档 token（`feishu_write_doc.py` 内 DOC_TOKEN）。
- `python` 缺 requests → 统一用 `py -3`。

## 追加：苏老师质询"是否其他文献/顶会无进展"的核查（同日 10:23 后）
**结论：不是无进展，是检索结构偏斜（arXiv 依赖度过高）。**
- 实测数据：本期 arXiv 17/27 = **63%**，为近 9 期最高（09-02 至 09-15 区间为 6%–54%，多数 12%–31%）。
- 反证实测（OpenAlex 官方 API，窗口 2026-09-06..09-20，`type:article` + 期刊 + ocean/marine/sea + deep learning）：**命中 225 篇期刊论文**，非空窗。
- 漏检样例（已用 `verify_paper.py` 核实元数据，均 hybrid OA）：
  - `10.1016/j.oceaneng.2026.128203` Ocean Engineering，深海采矿立管损伤识别（多尺度 SE-ResNet），大连海事大学，OpenAlex 在线首发 2026-09-18。
  - `10.1016/j.marpolbul.2026.120317` Marine Pollution Bulletin，语义子原型建模的遥感溢油检测，OpenAlex 在线首发 2026-09-18。
- **三条根因（均在 skill 层，非检索工具故障）**：
  1. 来源覆盖矩阵 A 组把 OpenAlex/Crossref 的检索方式写成"用 API（`verify_paper.py` 已内置）"，**角色被定义为"核验工具"而非"发现工具"** → 实战中只用于事后核实，未用于按主题+日期主动发现。
  2. 阶段一"必须产出 ≥5 条初筛"**未限定来源**，可用 arXiv 预印本顶数 → 顶会/IEEE 强制项被稀释（本期 IEEE Xplore 仍为零覆盖）。
  3. 时效规则对 Elsevier/Wiley 类"online first"期刊会误判：Crossref 返回的是**期号日**（本次实测 2026-10-01 / 2026-12-01），OpenAlex 返回**在线首发日**（09-18）。若只信 Crossref，会把最新期刊论文判为"未出版/超期"而剔除。
- **客观因素（须区分）**：9 月中旬是顶会周期空档（CVPR 6 月、ICCV 10 月、NeurIPS 12 月、IGARSS 7 月、OCEANS 9 月底），会议论文确实少；**但期刊不存在空档，这是我们可补的量**。
- **待苏老师拍板的修复方案**：①阶段二 A 组改为"发现式检索"（给出 Crossref/OpenAlex 主题×日期查询模板，每期≥2 组并记录命中数）；②阶段一 ≥5 条须至少 2 条来自会议论文或 IEEE 期刊，arXiv 不得全额顶数；③时效规则补"期刊 online-first 以 OpenAlex `publication_date` 为准"。另需决定是否补录本期漏检期刊论文。

## 追加：苏老师提出"全量 vs 每方向最多 5 篇"的矛盾，给出前沿跟踪架构方案（同日 10:35 后，**待批准**）
**核心判断**：日报是"精选摘要"，不是跟踪工具；"每方向 3-5 条"是**排版口径**而非覆盖口径。要"尽可能全面及时掌握动态"，必须把"发现"与"呈现"分离。
**实测量级（OpenAlex 官方 API，窗口 2026-09-06..09-20，`type:article`+期刊）**：
- 全海洋类：**2,249 篇**
- 海洋数字孪生/资料同化/数值模式类：80 篇
- 海洋+深度学习/神经网络类：225 篇
- 海洋AI/数字孪生/数据三方向合计（含重叠）：**≈350 篇** → 日报呈现 27 条，**留存率约 10%**。
**方案：四层架构**
- L0 采集层（新增）：`harvest.py`，9 方向查询模板 × Crossref/OpenAlex/arXiv API，每日全量落 `data/pool/YYYY-MM-DD.jsonl`；T+1 可达（OpenAlex/Crossref 支持 `from-created-date`）。
- L1 文献池（新增）：`screen.py` 去重（DOI/arXiv ID/标题指纹）+ 相关度分级（规则打分→S/A/B）+ 方向标签；产出 `data/library.db` 与 `data/pool_index.json`。S 级才是日报候选。
- L2 日报（**保持不变**）：27–45 条，仍逐条核链+摘要一致+去重，质量标准不降级。
- L3 新出口：①**前沿看板** `frontier.html` 上 GitHub Pages，静态 JSON + 前端检索/筛选（方向、时间、期刊、关键词），零后端；②**周报/月报** `weekly/` 由池内统计生成趋势综述（热点主题、方法迁移、机构分布）。
**待决策（推荐）**：①存储并入现有仓库 `data/`（推荐，Pages 可直接读，年增约 10MB）vs 独立仓库；②看板走静态 JSON+前端筛选（推荐）vs 静态列表页；③相关度阈值需苏老师提供 10–20 条"该收/不该收"样例作标注集校准，或先规则+人工复核一周再定；④周报节奏 weekly（推荐周一）vs biweekly。
**新增质量指标（建议写入 skill）**：每期日志记录 harvest 数 / 池内数 / 日报收录数（留存率），arXiv 占比目标 ≤50%，期刊来源占比目标 ≥40%。

## 追加：四层架构已落地实施（同日 11:10 后，苏老师批准"其他都按建议来"）
**苏老师拍板的期刊分级口径（三条原则）**：①期刊质量最高优先级，参考学会/高校/机构目录+影响因子，Science/Nature/PNAS 正刊及子刊为最高；②MDPI 等被多家机构列为预警的出版集团为最低等级并标注；③EI/SCI 预警与剔除期刊为最低等级并标注。相关度阈值采用**问答式校准**（自动收集待确认期刊，由苏老师逐条确认）。
**已交付工件**：
- `data/journal_tiers.json`：分级登记表（S/A/B/C/P/D/? + 权重 + 出版集团规则 + 预警名单）。已核实真实名单：中科院2025（5本，2025-03-19发布）、中信所2025（103本，本表已录14条相关条目）、中科院2024（存档，按官方规则不累积使用）。
- `tier_engine.py`：三级判定（登记表 → 最新一期预警名单 → 出版集团规则 → 指标自动评级 → 待确认），支持 `--pending`/`--stats`。
- `source_metrics.py`：按 ISSN 抓 OpenAlex Sources 指标（h-index/两年均被引/DOAJ/年发文）缓存至 `data/source_metrics.json`（已补齐 1,745 个 ISSN，命中率 100%）。
- `harvest.py`：L0 采集，9 方向 × OpenAlex（+会议论文 proceedings-article）+ arXiv；窗口 09-06~09-20 抓到 **3,468 条**（arXiv 因 429 限流未纳入，需下期重试）。
- `screen.py`：L1 筛选，去重（DOI/arXiv ID/标题指纹）+ 分级 + 相关度打分 + 历史比对 → **1745→170 条**入池；**未登记期刊 0 本**；分级分布 A67/B42/C51/D8/S2。
- `frontier.html`：前沿跟踪看板（全池可检索，方向/分级/时间/关键词/仅未收录过/隐藏预警刊筛选 + 三种排序）。已加入 index.html 与 archive.html 导航。
- `weekly_report.py` + `weekly/2026-W38.html`：L3 周报（方向分布、分级分布、高频期刊、预警刊清单、主题词、Top15、预留趋势综述段落）。
- skill 已同步：新增「前沿跟踪四层架构」整节 + 阶段一"≥5 条中至少 2 条来自会议/IEEE 期刊"来源限定 + 陷阱 18/19/20（arXiv 依赖、online-first 日期、预警刊不得静默剔除）。
**提交**：`b1675f9`（架构+引擎）、`9442ddf`（筛选收紧+周报），均已推送。
**已知待办**：①arXiv 429 限流需下期重试或用 WebFetch 兜底；②中信所2025完整103本名单待导入；③自动评级阈值（h-index 120/50/15、两年均被引 6/3/1）待苏老师按学科经验校准；④待确认期刊问答清单（本次为 0 本，说明登记表+指标已全覆盖）。

---

## 追加：分级口径七项决策落地（苏老师 2026-09-20 拍板）· 提交 `8eb7e52`

**七项决策与实现**
| 决策 | 实现 |
|---|---|
| Scientific Reports / Ocean Engineering 维持 A | `journal_tiers.json` 登记确认（去掉"待校准"） |
| npj Heritage Science 由 S 下调为 A | 登记表改 `tier: A` |
| 自动评级阈值**整体下调** | `tier_engine.auto_tier` 重写（见下） |
| Frontiers = C、MDPI = D 确认 | 集团规则保留；**并补 MDPI/Hindawi/Frontiers 出版方全称别名** |
| 中文刊按各学科影响因子排名分档 | 海洋学报 B、遥感学报 B、海洋科学 C、中国海洋大学学报 C（+ 别名/en 名）；口径写入 skill |
| **预印本若已对应期刊论文，按所属期刊等级排序** | `tier_engine.classify(preprint_journal=...)` 升级通道 + `screen.py` 四级匹配 |
| **SCI/EI 预警与剔除刊 → D** | 新增 `warning_lists.EI剔除2026`（19 条近期剔除）、`WoS剔除2026`（16 条） |

**阈值改动（按实测分布校准，非凭感觉）**
- 实测各档 h-index 中位：S 280 / A 210 / B 106 / C 54 → 海洋地学类整体低于生物医学，故 h 门槛下调。
- 新规则：A `h≥120` 或 `h≥70 且 c≥3.0`；B `h≥50` 或 `h≥30 且 c≥2.0` 或 `c≥4.0`；C `h≥8` 或 `c≥0.5`。
- **同时堵住反向漏洞**：原"仅凭两年均被引 ≥6.0 即进 A"曾把 Journal of Hospitality and Tourism Management、Essays in Biochemistry、F1000Research 之类判成 A，现该通道需同时满足 h≥70。
- 巨型刊防误判改为 `年发文>2500 且 两年均被引<8`（原先仅看发文量，会把 Advanced Materials 等真顶刊误降）。

**实测效果（池 3,932 条 → 183 条）**
- 分级：A 36 / B 64 / C 58 / S 2 / P 13 / **D 10**（原先 170 条时 A 67/B 42/C 51/D 8）。
- 自动评级"虚高进 A"基本消除；新增 D 命中：Marine Drugs、Diversity（MDPI 全称此前漏判）。
- 契合度分档（新）：标题即海洋主题 94 / 弱相关 71 / 海洋介质型 18。

**两个新发现（都已修）**
1. **arXiv 官方 export API 被 WAF 拦截**：`export.arxiv.org/api/query` 对全部 User-Agent 返回 **406**（此前 429），
   而 `arxiv.org/abs/*` 网页正常 → 是端点拦截，非网络故障。**改走 OpenAlex 索引的 arXiv 通道**
   （`locations.source.id:S4306400194`），实测 14 天窗口 464 条预印本，且 OpenAlex 的 `locations`
   能直接给出期刊版本 → 正好供"预印本升级"使用。同时修掉旧 bug：**arXiv 只对方向一发起检索**（现 9 方向全覆盖）。
2. **预印本升级在 OpenAlex 口径下"可见效果为 0"**：OpenAlex 会把预印本与期刊版**合并为同一条 work**，
   故 61 条带期刊版本的预印本中 49 条与池内期刊记录同标题被去重（预期行为），
   余 12 条为非海洋主题被相关性过滤。→ 规则已实现并单元验证（npj Ocean Sustainability→S、Frontiers→C、MDPI→D），
   其价值体现在"仅有预印本、期刊版尚未入库"的场景，以及未来按预印本首发日计的时效口径。

**海洋相关性判据（新增，解决看板噪声）**
- 池内噪声主因是**把海水当实验介质**的材料/化学/医学论文（海水电解、MXene 催化剂、铀吸附、水凝胶等），
  它们靠 A 级期刊权重挤进前列。
- 判据：**海洋"对象"词**（ocean/marine/coastal/bathymetry/seabed/fisheries/Arctic…）＝研究海洋；
  **海洋"介质"词**（seawater/water/wave/polar/tide）**+ 非海洋领域词**（催化剂/电池/临床/作物…）＝用海而非研究海 → `off`，降权 -150。
- `frontier.html` 默认筛选"排除降权项"，可切到"仅被降权项（复核用）"——**降权不删除**，保留可审计性。
- 修后看板 Top 14 已由海洋类占满（npj Ocean Sustainability、Nature Geoscience、Ocean Engineering、JGR:MLC、ESSD…）。

**遗留**：①中信所 103 本全表、WoS **ESCI** 剔除全表待补录；②`data/pending_journals.txt` 本次 5 本均为 1 篇的域外刊（ICCV/JASA/Algorithmica/Physics of Life Reviews/Biosensors→已按 MDPI 修正为 D），按口径不提问；③飞书文档 token 仍 404。

---

## 追加：前沿周报访问入口 + 综述板块（同日 14:13 后）

**苏老师问题**：如何访问前沿周报？可否给周报加一个基于本周全部期刊的综述板块，快速了解各领域主要进展。

### 1. 访问入口（此前周报"生成了但没人找得到"）
- 三处导航新增「前沿周报」：`index.html` / `archive.html` / `frontier.html` → `weekly/index.html`。
- 首页 `<main>` 之外新增顶部横幅卡片（放 main 外，避免被每日插入的 post-card 往下顶）。
- 新建 `weekly/index.html` 索引页，**由 `weekly_report.py` 每次运行自动重建**（扫描 `weekly/*.json`），列出全部期次 + 池量 + 分级分布 + 是否含深度综述。
- 线上：`https://tianyunsu.github.io/ocean-data-daily-report/weekly/index.html`

### 2. 综述板块（`weekly_report.py` 重构，163 → 约 520 行）
- **两层结构**：① 自动综述（脚本生成，始终有）——逐方向「主题簇（标题关键词聚类）+ 代表文献带链接 + 主要期刊」，
  只显示 ≥2 篇的簇，其他单篇单列；② 深度综述（人工/模型）——写 `weekly/synth/YYYY-Www.md` 后运行脚本自动注入页面顶部。
- **写作草稿**：脚本同时产出 `weekly/synth/YYYY-Www.draft.md`（各方向期刊分布 + 主题簇 + Top5 代表文献），撰写必须以此为依据。
- **样本口径**：综述样本 = 池内 **剔除 D 级预警/受限刊 + 剔除"已降权"（海洋介质型/弱相关）项** → 183 → 155 条；
  被剔除数量在统计区与逐方向注脚单列，**不删除、可审计**（与看板口径一致）。
- 新增参数 `--synth <md>` / `--no-draft`；md 渲染支持 `#/##/###`、`- 列表`、`> 引用`、`**加粗**`、`[文字](链接)`。
- 新增 `TERM_CN` 关键词→中文簇名词典（约 150 词），近义簇合并去重；新领域高频词需补进词典。

### 3. W38 深度综述已撰写并上线
- `weekly/synth/2026-W38.md`：①本期总体判断（4 条跨方向主线）②逐方向主要进展（9 节，含链接）③值得跟进的 5 条线索 ④口径说明。
- 主线归纳：方法从"上模型"转向"讲机理"（PINN/分布感知/黑箱拆解）；海浪与海冰最密集；ESSD/Palaeo3 成体系产出数据产品；开源工具迭代快于方法论文。

### 4. 关键教训（已写入 SKILL.md 陷阱 23/24）
- **综述里臆造链接实测发生 2 处**：凭 DOI 规则手写 → Pattern Recognition 水下偏振重建（真实 `…114827`，写成 `…113548`）、
  Frontiers 滑翔机 QC（真实 `…1905807`，写成 `…1922361`，而后者其实是另一篇台风论文）。
  → **铁律：综述每条链接必须回查 `library.db` 的 `url` 字段**，核验脚本异常须为 0 才可交付。
- 周报不能只有统计表，必须有综述板块（回答"各领域主要进展是什么"，而非只给可检索清单）。

### 5. 工具链变更
- `weekly_report.py`：新增 `write_index()`（重建 `weekly/index.html`）、`clusters()`、`md_to_html()`、`is_off()`；输出 `weekly/synth/*.draft.md`。
- SKILL.md 新增「### L3 前沿周报与综述板块」小节（访问入口表、两层结构、命令、链接核验铁律、口径区分）；
  阶段四新增第 7 步（每周日随当日流程产出周报）；陷阱新增 23（臆造链接）、24（只写统计不写进展）。
- `sync_skills.py install` 已把技能同步到用户目录（status 一致）。
